void main() {
  // define a value 10
  int age = 10;
  var age2 = 10;

  // define a value 'Henry'
  String name = 'Henry';
  var name2 = 'Henry';

  // define a value true (Preson is alive = true or dead = false)
  bool isAlive = true;
  var isAlive2 = true;

  print(age);
  print(age2);

  String testNull;
  print(testNull);
}